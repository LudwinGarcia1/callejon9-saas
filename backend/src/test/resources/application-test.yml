spring:
  datasource:
    url: jdbc:postgresql://localhost:${DB_PORT:5433}/callejon9_test
    username: callejon9_app
    password: ${DB_APP_PASSWORD:app_dev_pwd}

  flyway:
    user: callejon9_owner
    password: ${DB_OWNER_PASSWORD:owner_dev_pwd}
    clean-disabled: false

  jpa:
    hibernate:
      ddl-auto: validate
