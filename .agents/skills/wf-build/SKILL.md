---
name: wf-build
description: "Ejecuta de extremo a extremo una tarea aceptada en Callejón 9: preflight, implementación incremental, pruebas y reporte de evidencia, sin publicar cambios salvo autorización explícita."
---

# Build

Sigue `workflow/workflow.md`, sección `$wf-build`. Antes de editar, lee `AGENTS.md`, `workflow/project-config.yaml` y el brief. Activa las skills de dominio afectadas. Si no existe un criterio verificable, usa `$pm-validate-issue` primero. Detente ante una decisión que cambie materialmente el alcance; no ante detalles resolubles desde el repositorio.
